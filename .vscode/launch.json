{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug: bot.bot_client (module)",
            "type": "python",
            "request": "launch",
            "module": "bot.bot_client",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "justMyCode": true,
            "envFile": "${workspaceFolder}/venv",
            "env": {
                "PYTHONUNBUFFERED": "1",
                "PYTHONASYNCIODEBUG": "1"   // asyncio の詳細ログ（必要なければ削除可）
            }
        }
    ]
}
